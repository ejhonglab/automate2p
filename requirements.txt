# Note that you will need to run the pywin32_postinstall.py script this installs
# in your Python/.../Scripts directory (as administrator), wherever Python is
# installed. This is true if pywin32 is installed with pip, but may not be true
# if you use one of their installers, which may do this step for you.
pywin32
pytimeparse
PyYAML
